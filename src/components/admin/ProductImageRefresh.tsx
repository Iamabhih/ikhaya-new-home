import React from 'react';

interface ProductImageRefreshProps {
  productId?: string;
  onRefresh?: () => void;
}

export const ProductImageRefresh: React.FC<ProductImageRefreshProps> = () => {
  return null;
};

export default ProductImageRefresh;
